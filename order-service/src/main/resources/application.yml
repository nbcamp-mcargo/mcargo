spring:
  application:
    name: order-service
  config:
    import: optional:file:.env-order[.properties]

  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${ORDER_URL}
    username: ${ORDER_USER}
    password :  ${ORDER_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 19093

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/