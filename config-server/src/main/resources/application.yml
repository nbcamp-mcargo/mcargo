spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo

server:
  port: 19098

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false